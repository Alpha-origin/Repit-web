import NotFoundWidget from "@/widgets/not-found";
import * as S from "./style";

const NotFound = () => {
  return (
    <S.Page>
      <NotFoundWidget />
    </S.Page>
  );
};

export default NotFound;
